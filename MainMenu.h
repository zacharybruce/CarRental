#ifndef MAINMENU_H
#define MAINMENU_H

struct MainMenu{
    MainMenu();
    ~MainMenu();

    void show_menu(); // Shows main menu to user
    void browse_menu(); // Allows user to browse available cars
};

#endif